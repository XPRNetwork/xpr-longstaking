"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const eos_1 = require("./eos");
exports.nameProxyHandler = {
    get: function (obj, prop) {
        if (typeof prop === `string`) {
            if (prop in obj)
                return obj[prop];
            // otherwise treat the string as a symbol
            try {
                const symbolAsName = eos_1.convertSymbolCode2Name(prop);
                if (symbolAsName in obj)
                    return obj[symbolAsName];
            }
            catch (_a) { }
            // cannot do this because test libraries like jest call this object
            // if (!isName(prop)) {
            //   throw new Error(`Table scopes must be valid eosio::names, which "${prop}" is not.`)
            // }
        }
        return obj[prop];
    },
};
