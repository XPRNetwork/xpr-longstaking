/// <reference types="node" />
import SerialBuf from "../serializer/buffer";
import SectionSerializer from "./abstract-section";
export declare type TPermissionLinkObject = {
    account: string;
    code: string;
    message_type: string;
    required_permission: string;
};
export default class PermissionLinkObjectSerializer extends SectionSerializer<TPermissionLinkObject[]> {
    constructor(buf: SerialBuf, rowCount: number, sectionName: string);
    deserialize(): void;
    serialize(): Buffer;
}
