/// <reference types="node" />
import SerialBuf from "../serializer/buffer";
import SectionSerializer from "./abstract-section";
import { TAuthority } from '../../../types';
export declare type TPermissionObject = {
    parent: string;
    owner: string;
    name: string;
    last_updated: string;
    last_used: string;
    auth: TAuthority;
};
export default class PermissionObjectSerializer extends SectionSerializer<TPermissionObject[]> {
    constructor(buf: SerialBuf, rowCount: number, sectionName: string);
    deserialize(): void;
    serialize(): Buffer;
}
