/// <reference types="node" />
import SerialBuf from '../serializer/buffer';
import SectionSerializer from './abstract-section';
export declare type TAccountObject = {
    name: string;
    creation_date: number;
    abi: string;
};
export default class AccountObjectSerializer extends SectionSerializer<TAccountObject[]> {
    constructor(buf: SerialBuf, rowCount: number, sectionName: string);
    deserialize(): void;
    serialize(): Buffer;
    printData(): string;
}
