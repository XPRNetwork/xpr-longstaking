/// <reference types="node" />
import SerialBuf from '../serializer/buffer';
import SectionSerializer from './abstract-section';
export declare type TCodeObject = {
    code_hash: string;
    code: string;
    code_ref_count: string;
    first_block_used: number;
    vm_type: number;
    vm_version: number;
};
export default class CodeObjectSerializer extends SectionSerializer<TCodeObject[]> {
    constructor(buf: SerialBuf, rowCount: number, sectionName: string);
    deserialize(): void;
    serialize(): Buffer;
    printData(): string;
}
