/// <reference types="node" />
import SectionSerializer from "./section-serializers/abstract-section";
export default class SnapshotSerializer {
    static MAGIC_NUMBER: number;
    static deserialize(snapshot: Buffer): Map<string, SectionSerializer<any>>;
    private static createEmptySections;
    private static deserializeSections;
    static serialize(sections: Map<string, SectionSerializer>): Buffer;
}
