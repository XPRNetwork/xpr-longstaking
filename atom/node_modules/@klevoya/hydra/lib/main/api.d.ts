/// <reference types="node" />
import { TTransactionTrace } from "../types";
import { THydraConfigOptions } from "../config/hydra";
declare type PushTransactionResponse = {
    snapshot: Buffer;
    trace: TTransactionTrace;
};
export default class ServerApi {
    private options;
    private axios;
    constructor(options: THydraConfigOptions);
    private apiRequest;
    pushTransaction: (data: object) => Promise<PushTransactionResponse>;
}
export {};
